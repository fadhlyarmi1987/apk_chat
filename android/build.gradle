buildscript {
    ext.kotlin_version = '1.9.0' // Ganti dengan versi terbaru
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2' // Ganti dengan versi terbaru Android Gradle Plugin
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'com.google.gms:google-services:4.3.15' // Ganti dengan versi terbaru Google Services plugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
